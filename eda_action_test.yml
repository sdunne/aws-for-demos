---
- name: Initial test of EDA action
  hosts: "{{ _hosts }}"

  tasks:
    - name: Show event info
      ansible.builtin.debug:
        var: eventdata

    - name: Show _hosts var
      ansible.builtin.debug:
        var: _hosts

